import { Response } from "express";
import { IAuthenticatedRequest } from "../types/auth.types";

export const addProduct = async (req: IAuthenticatedRequest, res: Response) => {
    try {

        

    } catch (err) {

    }
}